#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <locale>
#include <Windows.h>

int main()
{
	float N, i, P;
	P = 0;
	scanf_s("%f", &N);
	for (i = 1; i <= 2*N - 1; i += 2) {
		P += i;
	}
	printf("%.0f \n", P);
}
