#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <locale>
#include <Windows.h>

int main()
{
	double N;
	float i, P;
	scanf_s("%lf", &N);
	setlocale(LC_ALL, "Rus");
	for (i = 1; i <= 10; i++) {
		P = i / 10;
		printf("%.1f кг конфет стоят %.2lf \n", i/10, P*N);
	}
}
