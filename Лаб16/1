#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <locale>
#include <Windows.h>
#include <malloc.h>

int main()
{
	int* a;
	int N, i;
	scanf_s("%d", &N);
	a = (int*)malloc(N * sizeof(int));
	for (i = 0; i < N; i++) {
		a[i] = 1 + 2 * i;
		printf("%d \n", a[i]);
	}
	return 0;
}
