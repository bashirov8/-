#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <locale>
#include <Windows.h>

int main()
{
	int A, B, i, j;
	scanf_s("%d", &A);
	scanf_s("%d", &B);
	for (i = A; i <= B; i++) {
		for (j = 1; j <= i; j++) {
			printf("%d \n", i);
		}
	}
}
